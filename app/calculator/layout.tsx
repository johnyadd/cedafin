/** See app/match/layout.tsx — same reason. */
export const metadata = {
  title: "Ghana investment calculator — what fees and the cedi do to your returns",
  description:
    "See how much a fund's charges take over time, and how much of a return came from the exchange rate rather than the fund. For Ghanaian and diaspora investors. Working out a Treasury bill? There's a separate calculator for that.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}