/** See app/match/layout.tsx — same reason. */
export const metadata = {
  title: "Ghana investment returns calculator — fees, currency and charges",
  description:
    "What a fund charge actually costs over time, and how much of a return was the exchange rate rather than the fund. Built for Ghanaian and diaspora investors.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}